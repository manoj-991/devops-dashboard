apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-dashboard-chart.fullname" . }}
  labels:
    {{- include "devops-dashboard-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devops-dashboard-chart.selectorLabels" . | nindent 4 }}
