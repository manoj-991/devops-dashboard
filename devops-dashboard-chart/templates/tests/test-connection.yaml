apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "devops-dashboard-chart.fullname" . }}-test-connection"
  labels:
    {{- include "devops-dashboard-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "devops-dashboard-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
